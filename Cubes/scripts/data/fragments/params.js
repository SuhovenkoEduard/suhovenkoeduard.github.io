const minvalue = 1;
const maxvalue = 6;
const cubesCounter = 2;
const timeout = 500;
const resultStylesPath = 'styles/result/configurations/';

const params = {
  minvalue,
  maxvalue,
  cubesCounter,
  timeout,
  resultStylesPath
};

export default params;