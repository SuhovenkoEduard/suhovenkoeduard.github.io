const minvalue = 1;
const maxvalue = 6;
const cubesCounter = 2;
const timeout = 500;
const resultStylesPath = 'styles/result/configurations/';
let canceled = false;

const params = {
  minvalue,
  maxvalue,
  cubesCounter,
  timeout,
  resultStylesPath,
  canceled
};

export default params;