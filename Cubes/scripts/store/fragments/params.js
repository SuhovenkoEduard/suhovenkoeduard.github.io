const minvalue = 1;
const maxvalue = 6;
const cubesCounter = 2;
const timeout = 300;
const resultStylesPath = 'styles/result/configurations/';
let started = false;

const params = {
  minvalue,
  maxvalue,
  cubesCounter,
  timeout,
  resultStylesPath,
  started
};

export default params;